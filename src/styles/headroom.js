import { css } from '@emotion/core';

const headroom = css`

  .headroom {
    
    display: flex;
    justify-content: space-between;
    flex-direction: row;
    width: 100%;
    padding: 1rem 1.5rem;
    position: fixed;
  }

  
`;

export default headroom;
